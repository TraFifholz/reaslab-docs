{
  "permissions": {
    "allow": [
      "Bash(git add:*)",
      "Bash(git commit -m 'Use pnpm in deploy workflow for compatibility:*)",
      "Bash(git push:*)",
      "Bash(git commit -m 'Remove non-existent image placeholders to fix build:*)"
    ]
  }
}
